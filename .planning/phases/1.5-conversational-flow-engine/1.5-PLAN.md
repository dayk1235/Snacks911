---
wave: 1
depends_on: []
files_modified:
  - src/ai/runtime/intentAgent.ts
  - src/ai/runtime/flowEngine.ts
  - src/app/api/ai/chat/route.ts
autonomous: true
---

# Phase 1.5: Conversational Flow Engine - Plan

## 1. intentAgent (solo detección de intención)
<task>
<read_first>
- src/ai/runtime/intentAgent.ts
</read_first>
<action>
Crear `src/ai/runtime/intentAgent.ts` y exportar la función `detectIntent(message: string)`.
Debe inicializar Gemini (`gemini-2.5-flash-lite`) y usar el prompt del sistema para devolver un JSON estricto con `{ intent, producto }`. Implementar un fallback/retry con prompt de auto-corrección si el JSON inicial de Gemini es inválido.
</action>
<acceptance_criteria>
- El archivo `src/ai/runtime/intentAgent.ts` exporta la función `detectIntent`.
- La respuesta respeta estrictamente el formato JSON requerido.
</acceptance_criteria>
</task>

## 2. flowEngine base (sin lógica compleja)
<task>
<read_first>
- src/ai/runtime/flowEngine.ts
</read_first>
<action>
Crear `src/ai/runtime/flowEngine.ts`.
Definir las interfaces `ChatState` (producto, paso) y `FlowResponse` (message, nextState).
Crear la firma de la función base `handleMessage(message: string, intentData: IntentResponse, state: ChatState): FlowResponse` (sin lógica interna todavía).
</action>
<acceptance_criteria>
- `src/ai/runtime/flowEngine.ts` existe y define `handleMessage` con la firma correcta.
</acceptance_criteria>
</task>

## 3. manejo de estado simple
<task>
<read_first>
- src/ai/runtime/flowEngine.ts
</read_first>
<action>
Modificar `handleMessage` en `flowEngine.ts` para gestionar el estado de forma inmutable.
Clonar el estado de entrada (`const nextState = { ...state }`).
Si `intentData.producto` existe y el usuario no tenía un producto guardado previamente, actualizar `nextState.producto` y mover el paso a `'seleccion'`.
</action>
<acceptance_criteria>
- `handleMessage` no muta el `state` original.
- El estado transiciona a `'seleccion'` y guarda el nombre del producto correctamente.
</acceptance_criteria>
</task>

## 3.5 validación de intent (seguridad)

<task>
<read_first>
- src/ai/runtime/flowEngine.ts
</read_first>
<action>
Agregar validación defensiva para `intentData` dentro de `handleMessage`.

Si `intentData` es null, undefined o no contiene `intent`:
- usar intent = "otro"

Ejemplo:
const intent = intentData?.intent || "otro";

Esto evita que el flujo falle si Gemini devuelve datos incorrectos.
</action>
<acceptance_criteria>
- El sistema nunca rompe si intentData es inválido
- Siempre hay un intent válido para el switch
</acceptance_criteria>
</task>

## 4. reglas de flujo (if/switch)
<task>
<read_first>
- src/ai/runtime/flowEngine.ts
</read_first>
<action>
Implementar un bloque `switch(intent)` en `handleMessage` para controlar completamente el flujo de ventas determinístico SIN usar llamadas a IA.
Reglas a implementar:
- Si el usuario eligió un producto (o tiene contexto) -> No recomendar otros de cero.
- Si confirma o dice "pedido" -> Avanzar paso a "cierre".
- Si no hay contexto -> Mostrar opciones de menú.
Retornar `FlowResponse` con el mensaje y `nextState`.

- Si el usuario YA tiene producto en estado:
  → ignorar nuevas recomendaciones generales

- Si el usuario dice "sí" y hay producto:
  → avanzar a confirmación (no pedir otra cosa)
  
</action>
<acceptance_criteria>
- El código en `flowEngine.ts` controla el flujo usando switch/if.
- Las respuestas generadas provienen de lógica estática interna, no de un generador LLM.
</acceptance_criteria>
</task>

## 5. integración con /api/ai/chat
<task>
<read_first>
- src/app/api/ai/chat/route.ts
- src/ai/runtime/intentAgent.ts
- src/ai/runtime/flowEngine.ts
</read_first>
<action>
Modificar el endpoint POST `src/app/api/ai/chat/route.ts`.
Reemplazar el sistema antiguo LLM completo por la nueva arquitectura.
1. Parsear el `message` y extraer el `state` (o iniciar uno en default).
2. Llamar `detectIntent(message)`.
3. Llamar `handleMessage(message, intentData, state)`.
4. Devolver la respuesta manteniendo compatibilidad con la UI actual enviando `{ text: resultMessage, state: nextState }`.
Asegurar bloque try-catch para fallback general si la detección falla.

Si no existe estado previo:

const state = {
  producto: null,
  paso: "inicio"
};
</action>
<acceptance_criteria>
- `route.ts` importa y utiliza `detectIntent` y `handleMessage`.
- El endpoint devuelve un objeto JSON válido con la llave `text` para mantener el frontend estable.
</acceptance_criteria>
</task>
