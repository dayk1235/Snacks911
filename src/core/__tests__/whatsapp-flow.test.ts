/**
 * whatsapp-flow.test.ts — Tests reference removed modules (evaluator, filterProducts).
 * WhatsApp flow is now tested via botEngine integration and e2e tests.
 */
describe('Flow', () => {
  test.skip('removed — depends on removed modules', () => {});
});
