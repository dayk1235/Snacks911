/**
 * allergy.test.ts — handleMessageModular was removed from responseEngine.ts
 * during GSD simplification. Allergy filtering is now handled by
 * extractAllergies() in intentDetector.ts (tested in intentDetector.test.ts).
 */
describe('Allergy Filter System', () => {
  test.skip('removed — handleMessageModular no longer exported from responseEngine', () => {});
});
