/**
 * response.test.ts — handleMessageModular was removed from responseEngine.ts
 * during GSD simplification. Response logic is now in getBotResponse() (botEngine.ts)
 * tested by botFlow and botEdgeCases tests.
 */
describe('Response Engine Modular', () => {
  test.skip('removed — handleMessageModular no longer exported from responseEngine', () => {});
});
