phase:A
task:0

OBJ:
habilitar type products sin romper sistema

---

STEP 1
editar ResponseOutput

agregar:
type?: 'text' | 'buttons' | 'products'
actions soporta image, price

---

STEP 2
en responseEngine

reemplazar returns por:

return {
  text,
  type: actions ? 'buttons' : 'text',
  actions,
  nextState
}

---

STEP 3
agregar products response

if intent === 'exploracion':

return {
  text: "elige 👇",
  type: 'products',
  actions: [
    { label, value, image, price }
  ],
  nextState
}

---

STEP 4
en OrderBot.tsx

Msg:
agregar type

---

STEP 5
setMsgs:

agregar type: output.type

---

STEP 6
render:

if m.type === 'products':
render cards horizontales
usar image + label + price

---

REGLAS:
no tocar runBot
no romper flujo
no eliminar text

---

END